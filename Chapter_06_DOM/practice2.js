// Q2).Create 3 divs with common class name - "box".Access them & add some unique text to each of them 

let allclass = document.querySelectorAll(".box");
console.dir(allclass);

// allclass[0].innerText = "new unique value 1";
// allclass[1].innerText = "new unique value 2";
// allclass[2].innerText = "new unique value 3";


let idx = 1;
for(cl of allclass) {
    cl.innerText =  `new unique value ${idx}`;     // we can also write like this
    idx++;
}

















































































































